extends: default

rules:
  indentation:
    indent-sequences: consistent
    spaces: 2 # Or 4, adjust as preferred
  line-length:
    max: 120
  # Disable document-start rule if triple-dash is not used
  document-start: disable 